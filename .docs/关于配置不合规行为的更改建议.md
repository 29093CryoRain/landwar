内置数据似乎是直接记录在代码里的, 不方便调整.
建议改为读取另一份外部文件(称默认配置文件为A, 这份新文件为B), 游戏导出发行版时, 根据开发者写好A, 复制一份, 作为发行版中的B, 如果玩家把配置A文件改坏了, 再读取默认B文件.
先评估上述方案的合理性, 不要无脑赞同.


对于已知键数据类型/数据范围/约束条件, 以注释的形式写在jsonc中(把现有json文件/读取代码改为jsonc的).
考虑用schema 校验 约束上述条件.
缺少键时, 警告.