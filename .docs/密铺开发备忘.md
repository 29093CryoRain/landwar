# 密铺开发备忘（P12 收尾 → 半正/Laves 密铺）

> 写给明天（半正密铺 + Laves 密铺开发）的自己。今天（2026-08-15）踩过的坑、验证过的
> 经验、明天必须注意的点，都在这。优先读 §1 和 §3。

## 0. 阶段状态

- **P12（六边形/三角形异种密铺）已完成**：几何（TilingGeom）、随机图生成、移动/战斗/特效
  密铺化、城市形状表、渲染、三密铺确定性基线，全部落地并有单测。
- 今天收尾的 4 件事：① 三角常规直边布局；② forceCoast 边缘/横纹修复；③ 三角城市形状
  语义重做；④ 三角列数减半。
- **明天**：半正密铺（Archimedean，多种正多边形顶点排布，如 3.3.3.4.4）+ Laves 密铺
  （半正的对偶，单种顶点、多种面）。

## 1. 今日踩坑（现象 → 根因 → 修复，按代价排序）

### 1.1 横纹 / 一片同向三角（用户："几乎只在勾选强制边缘为海时出现"）
- 试过 3 个方案都失败/被否：1 环平滑 + 三角对平均（用户明确否决）、抗锯齿重采样
  （粗场网格 + 双线性插值，只缓解）、回退最朴素逐格采样（作为基线）。
- **真正根因**：forceCoast 的**边缘衰减带**（①b）用格索引 `d = min(r, rows-1-r, c,
  cols-1-c)` 估距离，在六/三角上衰减带沿整行/整列垂直延伸 → 阈值量化后横贯整行成
  直线海陆边 = "横纹"；三角正/反格共享同一 (r,c) 减同一衰减 → 趋海被拉平 = "同向三角"。
- **修复**：①b 衰减改用**格中心世界坐标到矩形边界的欧氏距离**
  `d = min(wx, worldW-wx, wy, worldH-wy)`，edgeBand 以 baseCell（世界尺度）为单位。
- **教训**：用户实验给的条件（"只在 forceCoast 时出现"）是定位利器，第一时间用它缩小
  范围，别急着在噪声采样上做文章。

### 1.2 边缘陆（"最外围一圈海"的判定，反复 3 次才修对）
- 用户规范：勾选"强制边缘为海"且陆地比例 100% 时，**只有最外围一圈是海**。
- 试过 3 个判据，前两个都错：
  1. 格中心距边界 < rim → 六偶数行最右列中心距右缘 √3a≈1.075 漏判；
  2. 格多边形顶点贴边（≤eps）→ 六偶数行最右列右顶点距右缘 √3a/2≈0.537 不贴边，漏判
     （用户："完全没变"）。
- **真正语义 = 与方形一致的"索引外圈"**（用户规范的本意就是方形 d==0 的直接推广）：
  - hex：`r==0 || r==rows-1 || c==0 || c==cols-1`（**含凹进的偶数行最右列**）；
  - tri：`r==0 || r==rows-1 || i==0 || i==cols-1`（三角对索引，最左/最右对 = 视觉最外列带）。
- **教训（最重要）**：密铺下"到边界的几何距离"无法两全——hex 左第二列与右最列到边缘
  距离相同（都 √3a/2），几何距离永远分不清"该海/该陆"；**索引语义才是与方形行为一致的
  锚点**。凡是"外圈/第几圈"这类用户规范，先按索引想，别按欧氏距离想。
- 另一个教训：**别在修 A 时顺手改 B**。修复轮 4 为了横纹把 ②b 从索引改成世界距离，
  直接引入边缘陆回归。①b（衰减，世界距离）与 ②b（外圈，索引）职责不同，分开。

### 1.3 三角城市基建形状（"选取非常奇怪、有的不边连通"）
- 旧实现：形状表 = 世界偏移 `{dx,dy,orient}`，但解析只 `worldToCell(偏移点)`，
  **orient 字段完全没参与**；且 `recomputeCityGeometry` 强制锚下标为偶（恒正三角）。
  后果：锚为反格（finishTerrain 遍历任意格作锚）时形状整体错位；1 级城恒正。
- **新语义（用户定稿）**：**正锚模式**——表按"锚=正三角"定义；锚为反三角时
  `Map::shapeCells` 对 **dy 垂直镜像**（orient 随之翻转）→ 自动得到朝向变体。
  - L1 = 1 格（正/反 2 种）；L2 = 正 + 同列下方反（**公共边水平**，即正格 k0 底邻）；
  - L4 = 边长 2b 大三角 4 格（正/反 2 种）；L6 = 6 格正六边形（左右顶点、上下平边）；
  - L8 = L6 + 顶上正 + 底下反（偏移 `(0,2h)` 和 `(0,-2h/3)`，注意是 -2h/3 不是 -4h/3）。
- **教训**：形状表的"朝向/锚"字段必须**真正参与解析**；parity-free 用世界偏移（直边布局
  下斜邻列偏移 ±1 随行奇偶变，格坐标偏移不 parity-free，世界偏移才 parity-free）。

### 1.4 三角地图过大（"同参数下为何如此之大"）
- 旧语义：width = 列对数 → 每行 2·width 格 → cellCount/世界范围 ≈ 方形 2 倍。
- **修复**：width 语义改为**视觉列数（每行三角数）**，生成/配置时列对数 = `(width/2) & ~1`
  （强制偶：105→52、106→52、108→54）。cellCount = width×height 与方形一致。
- **两处必须同一公式**：`MapGenerator::generate` 与 `Map::configure`（否则 lwmap 头的
  cols 与 geom_.cols 不匹配，loadFromLwmap 报 size mismatch）。菜单"长"步进 ±2 + 强制偶。
- 六边形没有此问题：cellCount = cols×rows = width×height，与方形一致；只是世界矩形略扁
  （宽 1.075×、高 0.93×），观感接近。

## 2. 关键经验与纪律（明天沿用）

- **基线**：任何 config 序列化 / 生成 / 几何变化都会移动 `state_hash`（hash = 全状态
  序列化的 FNV-1a）。`config.toJson()` 里密铺形状表一变，**连方形基线一起动**（纯序列化，
  行为不变）。**改动隔离验证法**：若改动只在某分支（如 forceCoast=true），CLI 基线
  （false）不变 → 用基线是否移动来证明改动没污染其他路径。
- **测试**：行为变化先补单测再动代码（外圈恒海、L2 水平公共边、L4 正反模式、正/反锚
  连通、格数=等级数）。临时探针测试（tests/test_stripe_probe.cpp）验证完**删掉**，并
  还原 CMakeLists（**显式源列表，新文件必须手动加**）。
- **几何**：直边布局下"正格 k0 底 = 同列下反格"（跨行同列），斜邻同行列偏移 ±1 随
  行奇偶（p=r&1）；世界偏移取"目标格中心 − 锚格中心"（=中心差），偏移点落在格内部
  解析才稳定（贴边/贴顶点会被 worldToCell 扫描序归属到别的格）。顶点穿越（crossEdge
  返回 -1）用 nudge ε 后 worldToCell 重定位，确定性。
- **渲染**：描边必须用 `TilingGeom::cellEdge`（多边形顶点序 ≠ 邻接边序）；粗线段用
  `Renderer::fillThickSegment`（实心四边形，别用偏移平行线）；SDL_RenderGeometry 顶点
  缓冲上限 ~65535 → 分批提交（6000 顶点/批）；地图边界灰线线宽固定 2px 不随缩放。
- **工具链**：landwar.exe 在跑时链接报 `Permission denied`（先杀进程）；PowerShell
  变量名大小写不敏感（`$B=$b[...]` 会把字节数组覆盖成字节——工具脚本里命名要避开）；
  内联 pwsh 传参给 powershell -File 的转义容易坏，能用脚本文件就别内联。

## 3. 明日（半正/Laves）开发清单

- **TilingGeom 全接口都要实现**（这是今天最大的工作量来源，明天会再来一遍）：
  `cellCount / worldWidth / worldHeight / cellCenter / cellPolygon / cellEdge /
  worldToCell / rowRange / colRange / neighborRaw / neighbor / crossEdge`。
- **面积相等边长**：每种正多边形边长按"面积 = 1（正方格）"推导（与 P12 一致，hex
  a=√(2/(3√3))、tri b=2·3^(-1/4)）；半正密铺含多种面，各自边长都按面积 1 定，格间
  可能产生缝/重叠？**先想清楚**：半正密铺是同一顶点排布下多种正多边形无缝铺满平面，
  边长必须统一（同一条边两侧面积不同），"每格面积 = 1"在混合面下不可能同时满足——需
  与用户确认边长/面积基准（可能按"最小单元面积"或"面数加权"）。用户追加:按该密铺下的多边形的理论数量加权平均后的平均面积为1.
- **先定义世界矩形与环绕周期**（wrap 图：边界锯齿、H 偶/列偶闭合条件），再写
  worldToCell/邻接。
- **索引语义优先**（§1.2 的教训）：外圈/邻接/城市形状一律按"格索引/行列"理解，别用
  欧氏距离下判断；世界坐标只用于渲染和几何计算。
- **确定性**：worldToCell 扫描序、crossEdge 顶点规则、RNG 使用顺序都必须是固定顺序；
  新密铺进 `Config`（map.tiling 枚举 + 城市形状表）→ 基线必动，重算后更新
  `test_baseline.cpp`、`工程规范.md` §3、`开发计划.md` §0。
- **城市形状表**：沿用"正锚模式 + 锚朝向镜像"或按新几何重设计；每等级形状格数 = 等级数、
  边连通，单测锁定（参考 CityTriShapesConnectedBothModes）。
- **渲染/预览**：MapRenderer 多边形填充走 cellPolygon；MapPreview 凸多边形扫描线填充；
  cellEdge 描边；批次提交。
- **范围建议**：先只做 1-2 种半正密铺（如 3.3.3.4.4 或 4.8.8）+ 对应 Laves 对偶，跑通
  全链路再扩展，别一上来做全表。

## 4. 14 种密铺开发代码位置表（横向开发用）

> 2026-08-16 用户定夺：横向推进；先完整统计要改的代码位置。CityIconFitter 工具已先做。
> 视觉相关统一用户测试，不自行调用视觉功能；尽量复用六/三角代码。

| # | 文件 | 现状 | 需要改什么 |
|---|------|------|-----------|
| 1 | `src/core/GameDefs.h` | `TilingType` 只有 Square/Hex/Tri，`tilingName/tilingFromName` 只认 3 个 | 枚举加 14 种（7 半正 + 7 Laves）；名称/反查加全；**不给"不做"的 3.3.3.4.4、Laves 对偶 3.3.3.4.4 五边形预留编号** |
| 2 | `src/core/Config.h` | `City::TilingSet::levels` 为 `vector<int>`；`setFor/shapeFor` 只 switch Square/Hex/Tri；`City::Shape` 只有 cells | levels 改 `vector<double>`（浮点等级）；`setFor` 加 14 分支；`Shape` 增加 `textureLevel`（贴图等级，整数）与 `level`（实际等级，实数）或保持并行数组；渲染 `iconScale` 语义按贴图等级 |
| 3 | `src/core/Config.cpp` | 内置 shape 表只有 square/hex/tri；JSON 解析 `getInt` 等级、`parseTilingSet` 只 hex/tri；toJson 只 hex/tri | 新增 14 套内置形状表（半正用实数等级 + 贴图等级；Laves 用格数整数等级）；JSON 解析/toJson 泛化到全部 tiling；等级键用 `getNum` |
| 4 | `src/world/tiling/Tiling.h` | `TilingGeom` 只含 type/cols/rows；常量只有 hex/tri | 接口不变，但需为每类型定义 `cols/rows` 语义、世界周期、`cellCount`；`cellPolygon` maxVerts 上限 ≥12；考虑增加 `TilingSpec`/格子表生成（避免 14 套手写 switch 失控） |
| 5 | `src/world/tiling/Tiling.cpp` | 全接口 switch Square/Hex/Tri | 实现 14 种 `cellCenter/cellPolygon/cellEdge/worldToCell/rowRange/colRange/neighborRaw/neighbor/crossEdge`；顶点命中/nudge 语义沿用 |
| 6 | `src/world/tiling/CityIconFitter.*` | **已实现**（本次先做的小工具） | 后续把 `CityRenderer` 的贴图缩放接到本工具；补充半正/Laves 形状单测 |
| 7 | `src/world/Map.h` | `City::level` int；shape 查表按 int | `City::level` 改 double；增加 `textureLevel` 字段；`shapeCells/canPlaceCity/addCity` 签名改 double 或加贴图等级参数 |
| 8 | `src/world/Map.cpp` | `shapeCells` 只分支 Square/Hex/Tri；`finishTerrain` 回退硬编码 1 级；`placeCapitals` 硬编码 `addCity(1,...)`；lwmap tilingByte 只 1/2 | shapeCells 泛化（世界偏移 + `worldToCell` 解析对所有 tiling）；回退等级改为 `levels.front()`；首都注册用最小等级；`loadFromLwmap` tilingByte 扩展 3..16；`configure` 每 tiling 行列约束 |
| 9 | `src/world/MapGenerator.cpp` | `generateTiled` 只六/三；lwmap tilingByte 只 1/2；`generate()` 只 clamp 三角列减半 | 支持 14 种 lwmap；每 tiling 行列 clamp/强制条件；噪声采样沿用 `cellCenter` 世界坐标；`defaultPath` 名称；注意 cityDensity 语义（期望锚点尝试数 vs 实际城市数） |
| 10 | `src/sim/SpatialHash.cpp` | `queryAABB/queryCircle` 对 Tri 特判 2 个 o | 去掉 Tri 特判，改为 `TilingGeom` 提供通用的"行/列 → 格下标列表"或 `cellsInRow(r,c)`，否则 14 种都要特判 |
| 11 | `src/sim/systems/MovementSystem.cpp` | `edgeLineAngle` 只 Square/Hex/Tri | 为 14 种补齐边线角（可由 `cellEdge` 端点反算线角，最通用）；`moveArmyTiled` 本体通用 |
| 12 | `src/sim/systems/ProjectileSystem.cpp` | 密铺路径已通用 | 基本不用改（依赖 worldToCell/crossEdge/neighbor） |
| 13 | `src/sim/systems/EffectSystem.cpp` | `solveBomb` 密铺分支对 Tri 特判 2 个 o | 同 SpatialHash，改为通用格枚举；激光已通用 |
| 14 | `src/sim/systems/SpawnSystem.cpp` | 已按 `tiling()==Square` 分支 | 基本不用改 |
| 15 | `src/render/MapRenderer.cpp` | `drawTiled` 对 Tri 特判 2 个 o；`cellPolygon` max 6 | 改为通用格枚举 + `cellPolygon` max 12；山贴图尺寸：Laves/半正平均面积=1，仍可按 `cellPx`；边界灰线通用 |
| 16 | `src/render/MapPreview.cpp` | 已通用但 `vx[6]` | 数组扩到 12；其余通用 |
| 17 | `src/render/CityRenderer.cpp` | `c.level` 直接 `-1` 下标；贴图缩放用 AABB `boxW/boxH` | `texLevel = round(c.level)` 夹 1..9；10+ 统一贴图（新贴图未就绪用 9 级）；缩放改用 `CityIconFitter`（世界坐标矩形 → 屏幕）；细线外廓已通用 |
| 18 | `src/replay/Snapshot.cpp` | `c.level` 按 int 序列化；tiling 枚举在 config 里 | level 改 double；若 City 加 textureLevel 则进快照；向后兼容读旧档 |
| 19 | `src/replay/Headless.cpp` | CLI `--tiling` 解析/帮助可能只 3 种 | 支持 14 种名称与帮助文本 |
| 20 | `src/ui/Menu.cpp` | 下拉 3 项；长宽步进只 Tri/Hex | 下拉 14 项（不做的不显示）；每 tiling 长宽约束/步进；`generateRandomMap` 分发已通用 |
| 21 | `src/core/Simulation.cpp` | 方= BMP，其余= lwmap | 基本通用；确认 14 种都走 lwmap |
| 22 | `src/app/Application.cpp` | 点选显示 `gx = cellIdx % geom.cols` | 对非行主序 tiling 可能要改用 `TilingGeom::cellRowCol`（如果新 tiling 下标不是 r*cols+c） |
| 23 | `tests/test_tiling.cpp` | 只测方/六/三 | 每种新 tiling 加：cellCount、面积=1、邻接对称、环绕、crossEdge 顶点 nudge、worldToCell 扫描序 |
| 24 | `tests/test_city_icon_fitter.cpp` | **已新增** | 补充真实 14 种城市形状的贴图不越界/尽量大单测 |
| 25 | `tests/test_baseline.cpp`、`工程规范.md`、`开发计划.md` | 三基线 | 新 tiling 分支各自基线（方/六/三基线不应因 config 序列化新增键移动——注意隔离验证） |
| 26 | `CMakeLists.txt` | 显式源列表 | 新文件加入（CityIconFitter 已加） |

## 5. 进度快照（2026-08-16 深夜）

- **已完成**：
  - 10/14 地图形式可生成/加载/渲染/放城：square/hex/tri + Arch3464/3636/31212/4612/488
    + Laves3464/3636/31212/4612/488。
  - 表驱动 `TilingGeom`（矩形周期域，`data/tiling_specs_arch.json` / `tiling_specs_laves.json`）。
  - `CityIconFitter` 接入 `CityRenderer`（缓存）；`tower10.png` 10级贴图。
  - 城市形状表支持同级多变体（`shapeLevelIndex` + Map 确定性选变体）。
  - 完整城市形状表（10/10）：Arch488、Arch3636、Arch3464、Arch4612、Arch31212、Laves3636、Laves488、Laves3464、Laves31212、Laves4612。Laves31212 L6 三变体、Laves4612 L4/L12 各两变体。
  - 方/六/三角三基线已重跑并更新（0x680ffc124cab1728 / 0xeaf48e1e357ef44f / 0x4289e4fd85b9b00b）。
- **仍缺**：
  - Arch33434 / Arch33336 / Laves33434 / Laves33336 四种扭棱/五边形密铺（边传播对扭棱类不闭合；需换构造路线）。
- **已知方向问题**：当前 5 种 Arch/Laves 的生成朝向与 .docs/开发思路.txt 的方向规范存在 30°/90° 级别旋转偏差（例如 Arch3636 六边形顶点在正上下方而非正左右方）。几何/拓扑正确，视觉朝向待用户测试后决定是否统一旋转。
