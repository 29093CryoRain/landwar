# 清理清单（Phase 6 归档记录）

> 2026-08-27 按 `.docs/优化与修Bug方案.md` Phase 6 执行。
> 规则：**确定无用 → 删除；不确定 → 移入 `rubbish/`**。
> 本次以“归档”为主，不直接删除，保留可回溯性。

## 约定

从本阶段起，对旧功能做改动时，**无特殊说明，一律默认不用兼容旧格式，该删的就删**。
详见 `.docs/工程规范.md` 与 `.docs/优化与修Bug方案.md`。

## 已归档：tools/ → rubbish/tools/（66 个）

这些是历史上为半正/Laves 密铺推导、验证、渲染、临时修正而写的**一次性/探索性脚本**，
当前主流程和文档不再依赖；移入 `rubbish/tools/` 保留备查。

- 分析与探索：`analyze_*.py`、`explore_*.py`、`fine_search.py`、`spec_search.py`、`visualize.py` 等
- 构造与修正：`build_*.py`、`fix_*.py`、`correct_shapes.py`、`recon_snub.py`、`primitive_snub.py` 等
- 一次性导出/渲染：`export_*_shapes.py`、`render_*.py`、`merge_final.py`、`emit_data.py` 等
- 临时核对：`_overlay_tmp.py`、`snub_shapes_output.txt`

## 已归档：.docs/ → rubbish/docs/（4 个）

- `异种地图开发思路.txt` —— 历史密铺铺设思路草稿
- `新铺设方案实现思路.md` —— 旧铺设方案分析，已被现行实现取代
- `密铺边界模型设计.md` —— 早期边界模型设计草稿
- `异种地图开发杂项.txt` —— 历史杂项备忘

## 保留的文档（含用户指定保留）

- `科技开发列表.txt` —— 用户指定保留：未来将会实现
- `密铺块视图工具.md` —— 用户指定保留

## 保留的工具（19 个）

保留仍在主流程、文档或资产生成中可能使用的工具：

- `apply_city_icon_fit_scales.py`
- `check_stripes.ps1`
- `city_icon_fit_tool.cpp`
- `derive_city_shapes.py`
- `export_arch_specs.py`
- `export_laves_specs.py`
- `gen_army_sheet.py`
- `gen_city_shapes_json.py`
- `gen_golden_map.py`
- `gen_markers.py`
- `gen_probability_maps.py`
- `gen_snub_city_shapes.py`
- `gen_table_domain_params.py`
- `gen_terrain_markers.py`
- `measure_visual_radius.cpp`
- `perf_analysis.cpp`
- `preview_city_shape.py`
- `update_city_icon_fit_config.py`
- `verify_snub_shapes.py`

## 本轮额外完成（非归档）

- Linux 构建支持：
  - `CMakePresets.json` 增加 `linux` 预设；
  - `CMakeLists.txt` 支持 `_deps/entt/include` 作为 EnTT 兜底；
  - `src/ui/Menu.cpp` 增加非 Windows 文件枚举实现（`std::filesystem`），Windows 仍走 `FindFirstFileA`。
- 基线改为“语义基线”：
  - `tests/test_baseline.cpp` 不再使用完整 Snapshot hash；
  - 只统计每格势力归属 + 所有兵的类型/势力/位置/速度方向/速度；
  - config 序列化/纯渲染改动不再导致基线漂移。

## 后续可做（剩余项）

- 统一 `Config::toJson` 与 `data/city_shapes.json` 的数据结构，减少双份格式。

## 已完成的高风险清理

- 删除 config/loadFromJson/toJson 中旧格式兼容分支：
  - square 旧 `{level,w,h}` 解析已删除；
  - `anchorBaseMask` 旧字符串/数字写法已删除；
  - 旧 `render.city.iconScale` 已删除；
  - 旧实时 `CityIconFitter` 运行时路径已删除。
- 合并 `Map::shapeCells` 与 `Map::placeableVariants` 中重复的形状解析：
  - 新增 `Map::resolveShapeCells()`，两处统一调用。
