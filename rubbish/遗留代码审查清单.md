# 历史遗留与代码审查清单

> 审查日期：2026-08-28。范围：当前工作树的 `src/`、`tests/`、`tools/`、`data/`、CMake 与主要文档；忽略 `build-*` 内容和已归档的 `rubbish/`。
> 只列高置信度、可定位、值得处理的项目；已在 `.docs/优化与修Bug方案.md` 明确安排的需求不重复展开。
> 基线：重新构建 `build-release` 后，401 项测试全部通过；下列 bug 主要是现有测试缺口。

## 一、Bug

| ID | 优先级 | 位置 | 问题与最小处理 |
|---|---|---|---|
| B01 | P0 | `src/replay/Snapshot.cpp:214,232-237`；`src/core/Config.cpp:1053-1059,1213`；`src/app/Application.cpp:46-52,144-150` | **读档后配置与地图/渲染分叉。** 快照内 config 不含实际地图宽高，也不含外置 `city_icon_fits`；反序列化只恢复 `Map` 宽高，窗口相机仍按默认 105x95，城市图标拟合表也变空。恢复时同步有效地图尺寸，并让快照自带或重新加载拟合表；补“非默认尺寸窗口读档”和拟合表往返测试。 |
| B02 | P0 | `src/sim/systems/MovementSystem.cpp:105-167` | **反弹前提前修改山地状态/速度。** 山到海下海失败、山到敌方平地反弹、平地到敌方山地反弹时，兵仍在原格，但 `MountainState`、`Speed`、剩余移动量已按目标地形修改。进入判定成功后再提交状态，或反弹时回滚；测试同时断言位置、速度和山地状态。 |
| B03 | P0 | `src/replay/Snapshot.cpp:200-460` | **合法但缺字段/错类型的 v6 JSON 可抛异常或越界。** 版本检查后的大量 `at/get/数组下标` 未统一捕获和校验，`type` 还会直接索引 `config.units`。先完整校验到临时状态，再原子提交；补缺字段、短数组、非法 entity/faction/unit type 测试。 |
| B04 | P0 | `src/core/Config.cpp:680-975`；`src/sim/systems/EffectSystem.cpp:51,120`；`src/app/Application.cpp:753-755` | **配置只有键名校验，没有类型/数值域校验。** `conquerEveryTicks=0`、`checkEveryTicks=0` 会取模零；`tickRate<=0` 会除零或使固定步长循环失效；部分数组元素错类型会抛异常。增加集中式 `Config::validate()`，加载失败应明确报错而不是带病运行。 |
| B05 | P1 | `tools/city_icon_fit_tool.cpp:182-209`；`src/world/Map.cpp:648-662` | **城市拟合工具与游戏使用了不同的形状协议。** JSON 的 `cells` 已是世界偏移，游戏直接相加；工具却对 hex/tri 再乘密铺间距，可能写出错误拟合数据。工具应直接复用 `Map::shapeCells/resolveShapeCells`，不要维护第二套解析。 |
| B06 | P1 | `src/core/Simulation.h:221-223`；`src/core/Simulation.cpp:291-308`；`src/replay/Snapshot.cpp:41-197` | **`unificationEmitted_` 未入快照。** 统一后读档再推进会重复发统一事件并改写统计截止 tick。序列化该状态，或从 `stats.cutoffTick` 和存档状态可靠重建；补统一后续跑测试。 |
| B07 | P1 | `src/sim/systems/EffectSystem.cpp:120-152` | **地雷在超时 tick 可生成两次爆炸。** 默认超时 3600 同时是探测周期 12 的倍数，附近有敌兵时先触发、后超时又生成一次。探测成功后直接结束，或将超时分支改为 `else if`。 |
| B08 | P1 | `src/world/Map.cpp:125-165`；`src/world/MapGenerator.cpp:308-337` | **BMP 读写都固定每行 1 字节 padding。** 非 105 宽的标准 24-bit BMP 会错行/截断，生成器也会写出不符合 4 字节对齐规则的文件。统一解析/写入宽高、像素偏移、方向和 `stride=(w*3+3)&~3`，并拒绝不支持格式。 |
| B09 | P1 | `src/ui/panels/PanelManager.cpp:50-75` | **面板在 `ImGui::End()` 后读取位置/大小。** 此时 current window 已恢复为父窗口，持久化的布局可能不是面板自身。把 `GetWindowPos/GetWindowSize` 移到 `End()` 前；补可注入的布局采集测试或手工回归。 |
| B10 | P2 | `src/app/Application.cpp:329-340` | **排行榜把 Projectile 计入兵力。** 统计遍历 `UnitType+FactionId`，没有像 `Simulation::countArmies` 一样排除子弹。直接复用 `countArmies` 或统一共享谓词。 |
| B11 | P2 | `src/app/Application.cpp:387-407`；`src/ui/DebugPanel.cpp:386-393` | **异种密铺格详情丢失基础格编号。** 点选只保存 `(row,col)`，展示再调用方格接口 `map.at(x,y)`；tri/arch/laves 可能显示同域内另一个格。Selection 保存 `cellIndex`，展示统一用 `atIndex`。 |
| B12 | P2 | `src/world/MapGenerator.cpp:121-132`；`src/ui/Menu.cpp:144` | **随机地图预览缓存键不完整。** 文件名遗漏 `forceCoast`，密度仅保留两位，小幅参数变化可能复用旧纹理。缓存键使用完整规范化参数；生成路径至少加入 `forceCoast` 和无歧义参数编码。 |
| B13 | P2 | `src/replay/Cli.cpp:78-85`；`src/replay/Headless.cpp:59-63` | **无参数的 `--replay` 使用随机种子。** CLI 文案把 replay 定义为确定性重跑，但未附 seed 时 `seedSet=false`，最终走时间随机种子。规定默认 seed（现有字段默认 42）并补 CLI 端到端测试。 |

## 二、可删除/收缩的遗留项

| ID | 置信度 | 位置 | 建议 |
|---|---|---|---|
| D01 | 确认 | `src/world/Map.cpp:628-631`；`src/core/Simulation.cpp:58-60`；`src/world/Map.h:122-123,188-189` | 删除空的 `Map::finalize()`、调用和测试调用；删除 Map 内只写不读的 `blockSize_/panelWidth_` 及 getter。 |
| D02 | 确认 | `src/core/Options.h:45`；`src/core/Options.cpp:74-90,129-140`；`README.md:114-120,128,146` | `wrap` 功能已从运行时移除，只剩 options 持久化、测试和旧文档。删除字段/序列化/测试，并清理 README 与开发文档中的可用功能描述。 |
| D03 | 确认 | `src/core/GameDefs.h:152-159`；`src/render/SpriteSheet.cpp:8-25`；`tools/gen_army_sheet.py:1-37` | 特殊兵贴图运行时已停用。删除 `kSpecialUnitFaction`、仅验证它的测试和 `army_special.png`；删除或重写旧生成器。当前生成器还会把 11 行 `army_base.png` 截成 8 行，属于危险工具。 |
| D04 | 用户否认 | `data/city.png`、`data/tower/city.png`、`data/towers.png`；`tools/gen_terrain_markers.py:2,64-65` | 当前运行时不加载这些旧城市资产，可删除；同步移除生成器中仍输出 `city.png` 的旧路径。`data/arrow2.png` 对应已规划功能，暂不归入删除项。用户补充：不删除贴图资产 |
| D05 | 确认 | `tools/update_city_icon_fit_config.py:5-8,45-47`；`tools/gen_golden_map.py:113-117`；`data/city_shapes_json_processor.py` | 删除重复的城市拟合旧入口及裸 JSON 兼容分支、未调用的 `sample_level()`、孤立且帮助文本已失效的 JSON 格式化脚本。 |
| D06 | 高 | `src/replay/Snapshot.cpp:208-210,240-269,312-460` | 只接受 v6，却保留大量“旧档缺字段”默认分支；它们不能读取真正旧版本，只会接受残缺 v6。按项目“不兼容旧格式”约定，改为严格 schema 后删除这些分支。 |
| D07 | 确认 | `build-asan/`；`data/.claude/settings.local.json`；`.gitignore:1-16` | 仓库仍跟踪 CMake 生成物和机器权限配置。移出版本控制，并用 `/build-*/`、`**/.claude/` 覆盖同类路径。 |
| D08 | 确认 | `README.md:17-34,76,114-120,174-175,206-215,234-238`；`CMakeLists.txt:188-190` | 文档仍引用已删除批处理、根目录 exe、wrap、旧城市/特殊兵贴图和 20k tick 基线；统一按当前脚本、资产和 1000 tick 测试更新。 |

## 三、值得复用的代码

| ID | 优先级 | 重复位置 | 最小收敛方案 |
|---|---|---|---|
| R01 | 高 | `MovementSystem.cpp:286-375`；`ProjectileSystem.cpp:107-155`；`EffectSystem.cpp:155+` | 提取 `TilingGeom` 射线逐格推进器，统一 `crossEdge`、顶点 nudge、邻格/出界和防死循环；调用方只处理反弹、销毁、击杀。 |
| R02 | 高 | `Map.cpp:688-728,731-795` | 提取单城 `updateCityGeometry(City&)`；新建城市和读档重算复用，避免中心/AABB/方形宽高规则漂移。 |
| R03 | 高 | `Map.cpp:125-167`；`MapGenerator.cpp:90-113,308-337`；`tests/TestUtil.h:29+`；多份地图测试 | 建立共享 BMP24 codec/stride 计算；同时把 terrain ramp/通道编码集中成 `TerrainCodec`，避免配置可调而生成器/测试仍硬编码 `32/128/127`。 |
| R04 | 中 | `ProjectileSystem.cpp:22-27`；`EffectSystem.cpp:33-38`；`Headless.cpp:40-48`；`Simulation.cpp:249+` | 复用 `maxArmyRadius` 和现有 `Simulation::countArmies`，统一“活兵不含 Projectile/Dead”的口径。 |
| R05 | 中 | `Map.cpp:648-665`；`CityRenderer.cpp:201-220`；`city_icon_fit_tool.cpp:182-260` | 城市形状解析和外边界提取做成唯一公共实现；游戏渲染、拟合工具和测试只消费结果。B05 修复时一并完成。 |

## 四、建议执行顺序

1. 先修 B01-B05，并为每项补失败用例；这些涉及读档安全、状态正确性和工具产物可信度。
2. 再修 B06-B09；其中快照格式变更集中一次完成，版本号只升级一次。
3. 处理 D01-D08；删除后全仓搜索旧符号/资产名，并同步 README 与发行文档。
4. 按 R01-R05 收敛共享逻辑；不要在修 bug 前做大范围抽象，以便测试能证明行为变化来自修复本身。
5. 每批执行 `cmake --build build-release` 与 `ctest --test-dir build-release --output-on-failure`；模拟行为变化同步确定性基线。
