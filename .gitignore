# 构建产物
/build/
/build-release/

# 本地第三方源码 (FetchContent 本地源)
/_deps/

# ImGui 运行时配置
imgui.ini

# VS Code
.vscode/

# 本地 agent 配置（机器/会话相关）
/.claude/

# 根目录可执行副本（构建产物拷贝，供项目根运行）
/landwar.exe

# 运行期产物（2026-08 工程改进：统一入 userdata/，可丢弃、不进版本库）
#   userdata/options.json    菜单选项（开始游戏时保存）
#   userdata/maps/gen_*.bmp  随机地图生成物（P6，确定性：seed+参数可再生成）
#   userdata/screenshots/    F12 / QA 钩子截图
/userdata/

# 运行时截图（旧位置，兼容清理）
/screenshot_*.png
/shot_*.png
/beam_*.png

# run_test.bat 输出
/out_capture.txt

# Python 工具缓存
__pycache__/
*.pyc
